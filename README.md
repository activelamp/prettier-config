#  ActiveLAMP [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @activelamp/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@activelamp/prettier-config"
}
```
